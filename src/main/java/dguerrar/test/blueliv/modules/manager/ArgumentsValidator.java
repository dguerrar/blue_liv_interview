package dguerrar.test.blueliv.modules.manager;

import dguerrar.test.blueliv.dto.ArgumentsDTO;

public interface ArgumentsValidator {
	/***
	 * validate the arguments from MAIN
	 * 
	 * @param dto
	 * @return
	 */
	public ArgumentsDTO validate(ArgumentsDTO dto);

}
